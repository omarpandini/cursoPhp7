//Iniciando o git em uma pasta / Primeiro uso
git init

//Clonando um repositório
$ git clone git://github.com/schacon/grit.git

//Verificando objetos alterados
git status

git diff

//Adicionando para o stage area
git add --all

//Fazendo o commit
git commit -m "mensagem do porque"

//Jogando para o servidor
git push origin master

